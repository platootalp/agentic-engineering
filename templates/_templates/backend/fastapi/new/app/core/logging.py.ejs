---
to: "<%= projectName %>/backend/app/core/logging.py"
---
import logging
import sys

logging.basicConfig(
    level=logging.INFO,
    format="%(levelname)-8s [%(name)s] %(message)s",
    handlers=[logging.StreamHandler(sys.stdout)],
)
