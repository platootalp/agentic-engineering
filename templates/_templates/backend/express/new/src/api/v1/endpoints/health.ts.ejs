---
to: "<%= projectName %>/backend/src/api/v1/endpoints/health.ts"
---
import { Router } from 'express'

export const healthRouter = Router()

healthRouter.get('/health', (_req, res) => {
  res.json({ detail: 'ok' })
})
